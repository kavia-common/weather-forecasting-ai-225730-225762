# weather-forecasting-ai-225730-225762

Frontend now fetches weather directly from Open‑Meteo (no backend/proxy required) and opens a dedicated Search & Forecast page by default.
See `frontend_react_weather/README.md` for usage details and environment configuration examples.